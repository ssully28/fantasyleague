import React from 'react';
import classes from './Standings.module.css';

const Standings = (props) => {
  return (
    <div className={classes['Standings']}>I am Standings</div>
  );
};

export default Standings;
